[project]
name = "gongzuo-wendang-dazi"
version = "0.1.0"
description = "工作文档搭子：一个优先接入飞书等办公平台的项目文档记忆助手。"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "工作文档搭子 Contributors" }]
dependencies = []

[project.optional-dependencies]
parsers = [
  "beautifulsoup4>=4.12",
  "pypdf>=4",
  "python-docx>=1.1",
]

[project.scripts]
wendang-dazi = "work_memory.server:main"

[tool.ruff]
line-length = 100
